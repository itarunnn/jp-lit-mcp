---
plan: "04-04"
title: "fixture + unit テスト追加（mapSearch / mapRecord / adapter / filters validation）"
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - tests/fixtures/nihu-bridge/search-response.json
  - tests/fixtures/nihu-bridge/record-response.json
  - tests/nihuBridge.adapter.test.ts
  - tests/jpLitSearch.test.ts
autonomous: true
---

<objective>
nihuBridge mapper / adapter / Zod スキーマの単体テストを追加する。fixture は調査レポートと API 仕様に基づく合成 JSON で、live API なしで検証可能にする。

Purpose: live smoke が無くても CI で正規化ロジック（fields ルックアップ、`||` 区切り、temporal の `,` 区切り、404 フォールバック、POST body 構築、filters validation）を検証する。
Output: fixture 2 件 + adapter テスト 1 件 + jp_lit_search の filters.nihu_bridge validation テスト追加。
</objective>

<context>
- 既存パターンは `tests/jdcat.adapter.test.ts`（fixture 読み込み + mapper 単体 + adapter のグローバル fetch stub）を倣う。
- fixture は調査レポートに掲載の構造に従って合成する（live レスポンスを丸コピーする必要はない）。最低限の hits 1 件と researchResource 1 件で mapper が期待値を返すことを示せれば十分。
- `tests/jpLitSearch.test.ts` には `searchInputSchema` の filters.nihu_bridge validation テストを 3 件追加（accept / reject when source !== nihu_bridge / reject when no source）。これは既存 irdb の同型テスト L733-765 に倣う。
</context>

<interfaces>
依存する既存実装:

```typescript
// src/sources/nihuBridge/mapSearch.ts
export function mapNihuBridgeSearchResponse(payload: unknown): { total: number; items: SearchItem[] };
export function mapNihuBridgeSearchHit(hit: { database: string; id: string; fields: Array<{field:string,label?:string,value:unknown}>}): SearchItem;

// src/sources/nihuBridge/mapRecord.ts
export function mapNihuBridgeRecordResponse(payload: unknown): RecordItem | null;

// src/sources/nihuBridge/adapter.ts
export function createNihuBridgeAdapter(opts?: { searchUrl?: string; recordBaseUrl?: string }): SourceAdapter;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: fixture JSON を 2 件作成</name>
  <files>
    tests/fixtures/nihu-bridge/search-response.json,
    tests/fixtures/nihu-bridge/record-response.json
  </files>
  <read_first>
    - tests/fixtures/jdcat/search-response.json (fixture の規模感の参考)
    - tests/fixtures/jdcat/record-response.json (同上)
    - docs/research/phase4-nihu-bridge.md §2.5 / §2.6 (検索 / 取得レスポンスのスキーマ)
    - docs/research/phase4-nihu-bridge.md §7.3 (title/creator/temporal/link のフォーマット例)
  </read_first>
  <action>
    1. `tests/fixtures/nihu-bridge/search-response.json` を新規作成。1 件の hit を含む合成 JSON:
       ```json
       {
         "info": {
           "statusCode": 0,
           "total": 4621,
           "databases": {
             "nijl_nihonkotenseki": 4621
           }
         },
         "hits": [
           {
             "database": "nijl_nihonkotenseki",
             "id": "100000123",
             "fields": [
               { "field": "title", "label": "タイトル", "value": ["武家百人一首||ブケヒャクニンイッシュ"] },
               { "field": "alternativeTitle", "label": "別タイトル", "value": null },
               { "field": "creator", "label": "作成者", "value": ["源義経 <NDL_ID:00012345>"] },
               { "field": "publisher", "label": "所蔵者・組織", "value": ["国文学研究資料館"] },
               { "field": "subject", "label": "主題", "value": ["和歌||ワカ"] },
               { "field": "keyword", "label": "キーワード", "value": ["百人一首"] },
               { "field": "description", "label": "概要", "value": ["江戸期に編纂された武家による百人一首集"] },
               { "field": "temporal", "label": "対象時間範囲", "value": [{ "description": ["江戸時代"], "date": "1600-01-01T00:00:00+09:00,1868-12-31T00:00:00+09:00" }] },
               { "field": "spatial", "label": "対象空間範囲", "value": null },
               { "field": "datePublished", "label": "登録日", "value": "2022-07-20" },
               { "field": "researchResourceId", "label": "研究資源ID", "value": "100000123" },
               { "field": "originalId", "label": "オリジナルID", "value": ["nijl-12345"] },
               { "field": "inLanguage", "label": "言語", "value": ["jpn"] },
               { "field": "type", "label": "研究データのタイプ", "value": ["古典籍"] },
               { "field": "link", "label": "研究データへのリンク", "value": [{ "type": "原本", "link": "https://kotenseki.nijl.ac.jp/biblio/100000123" }] }
             ]
           }
         ]
       }
       ```
    2. `tests/fixtures/nihu-bridge/record-response.json` を新規作成。`researchResource` を含む合成 JSON:
       ```json
       {
         "info": {
           "statusCode": 0,
           "total": 1
         },
         "researchResource": {
           "databaseId": "nijl_nihonkotenseki",
           "researchResourceId": "100000123",
           "doi": null,
           "originalId": ["nijl-12345"],
           "title": ["武家百人一首||ブケヒャクニンイッシュ"],
           "alternativeTitle": ["武家百人一首抄"],
           "creator": ["源義経 <NDL_ID:00012345>"],
           "contributor": ["編者A"],
           "publisher": ["国文学研究資料館"],
           "subject": ["和歌||ワカ"],
           "keyword": ["百人一首"],
           "description": ["江戸期に編纂された武家による百人一首集"],
           "dateCreated": null,
           "datePublished": "2022-07-20",
           "temporal": [
             { "description": ["江戸時代"], "date": "1600-01-01T00:00:00+09:00,1868-12-31T00:00:00+09:00" }
           ],
           "spatial": null,
           "link": [{ "type": "原本", "link": "https://kotenseki.nijl.ac.jp/biblio/100000123" }],
           "type": ["古典籍"],
           "license": ["CC BY 4.0"],
           "inLanguage": ["jpn"]
         }
       }
       ```
  </action>
  <verify>
    <automated>
      ls "tests/fixtures/nihu-bridge/search-response.json" "tests/fixtures/nihu-bridge/record-response.json" && node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/nihu-bridge/search-response.json','utf8')); JSON.parse(require('fs').readFileSync('tests/fixtures/nihu-bridge/record-response.json','utf8')); console.log('valid')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/nihu-bridge/search-response.json` が存在し、有効な JSON。
    - `tests/fixtures/nihu-bridge/record-response.json` が存在し、有効な JSON。
    - search fixture に `"hits"` 配列が含まれ、最低 1 件の hit を持つ。
    - record fixture に `"researchResource"` キーが含まれる。
    - `node -e "JSON.parse(...)"` で両ファイルがパース可能。
  </acceptance_criteria>
  <done>合成 fixture 2 件が tests/fixtures/nihu-bridge/ 以下に揃った</done>
</task>

<task type="auto">
  <name>Task 2: nihuBridge.adapter.test.ts — mapper / adapter / POST body の単体テスト</name>
  <files>tests/nihuBridge.adapter.test.ts</files>
  <read_first>
    - tests/jdcat.adapter.test.ts (fetch グローバル stub による adapter テストの完全な構造)
    - tests/irdb.adapter.test.ts (filters のテスト方法 — 既存 irdb で URL params をチェックしている)
    - src/sources/nihuBridge/mapSearch.ts (mapNihuBridgeSearchResponse の正確なシグネチャ)
    - src/sources/nihuBridge/mapRecord.ts (mapNihuBridgeRecordResponse の戻り値型)
    - src/sources/nihuBridge/adapter.ts (createNihuBridgeAdapter の options シグネチャ、buildBody の挙動)
    - src/lib/date.ts (`normalizeIssuedAt` の戻り値仕様 — `issued_at: "1600-01-01"` のアサーション根拠)
  </read_first>
  <action>
    `tests/nihuBridge.adapter.test.ts` を新規作成する:

    ```typescript
    import { readFileSync } from "node:fs";
    import { afterEach, describe, expect, it, vi } from "vitest";

    function readFixture(name: string): unknown {
      return JSON.parse(
        readFileSync(new URL(`./fixtures/nihu-bridge/${name}`, import.meta.url), "utf-8")
      );
    }

    afterEach(() => {
      vi.unstubAllGlobals();
      vi.restoreAllMocks();
    });

    describe("nihuBridge mappers", () => {
      it("検索レスポンスを SearchItem に正規化する（title の || 区切りと creator の <ID> マークアップを除去）", async () => {
        const fixture = readFixture("search-response.json");
        const { mapNihuBridgeSearchResponse } = await import(
          "../src/sources/nihuBridge/mapSearch.js"
        );
        const result = mapNihuBridgeSearchResponse(fixture);

        expect(result.total).toBe(4621);
        expect(result.items).toHaveLength(1);
        const item = result.items[0]!;
        expect(item.source).toBe("nihu_bridge");
        expect(item.source_id).toBe("100000123");
        expect(item.title).toBe("武家百人一首");
        expect(item.title_reading).toBe("ブケヒャクニンイッシュ");
        expect(item.authors).toEqual([{ name: "源義経", role: "author" }]);
        expect(item.publisher).toBe("国文学研究資料館");
        expect(item.url).toBe("https://kotenseki.nijl.ac.jp/biblio/100000123");
        expect(item.availability.online).toBe(true);
        expect(item.material_type).toBe("古典籍");
        expect(item.subjects).toEqual(["和歌", "百人一首"]);
        expect(item.summary).toContain("武家による百人一首集");
        // temporal の開始日を issued_at に採用
        expect(item.issued_at).toBe("1600-01-01");
      });

      it("取得レスポンス（researchResource）を RecordItem に正規化する", async () => {
        const fixture = readFixture("record-response.json");
        const { mapNihuBridgeRecordResponse } = await import(
          "../src/sources/nihuBridge/mapRecord.js"
        );
        const record = mapNihuBridgeRecordResponse(fixture);

        expect(record).not.toBeNull();
        expect(record!.source).toBe("nihu_bridge");
        expect(record!.source_id).toBe("100000123");
        expect(record!.title).toBe("武家百人一首");
        expect(record!.title_reading).toBe("ブケヒャクニンイッシュ");
        expect(record!.authors).toEqual([{ name: "源義経", role: "author" }]);
        expect(record!.publisher).toBe("国文学研究資料館");
        expect(record!.alternative_titles).toContain("武家百人一首抄");
        expect(record!.subjects).toEqual(["和歌", "百人一首"]);
        expect(record!.language).toBe("jpn");
        expect(record!.material_type).toBe("古典籍");
        expect(record!.url).toBe(
          "https://kotenseki.nijl.ac.jp/biblio/100000123"
        );
        expect(record!.identifiers).toMatchObject({
          research_resource_id: "100000123",
          database_id: "nijl_nihonkotenseki",
          original_ids: ["nijl-12345"]
        });
        expect(record!.source_metadata).toMatchObject({
          database_id: "nijl_nihonkotenseki",
          research_resource_id: "100000123",
          license: "CC BY 4.0"
        });
        expect(record!.issued_at).toBe("1600-01-01");
      });

      it("researchResource が無いレスポンスでは null を返す", async () => {
        const { mapNihuBridgeRecordResponse } = await import(
          "../src/sources/nihuBridge/mapRecord.js"
        );
        expect(mapNihuBridgeRecordResponse({ info: { statusCode: 99 } })).toBeNull();
      });
    });

    describe("createNihuBridgeAdapter", () => {
      it("検索は POST + JSON body で叩き、paging.start を (page-1)*limit に揃える", async () => {
        const searchFixture = readFixture("search-response.json");
        const fetch = vi.fn().mockResolvedValueOnce({
          ok: true,
          headers: { get: () => "application/json; charset=utf-8" },
          json: async () => searchFixture
        });
        vi.stubGlobal("fetch", fetch);

        const { createNihuBridgeAdapter } = await import(
          "../src/sources/nihuBridge/adapter.js"
        );
        const adapter = createNihuBridgeAdapter();
        const result = await adapter.search({
          query: "源氏物語",
          limit: 10,
          page: 2
        });

        expect(fetch).toHaveBeenCalledTimes(1);
        const [calledUrl, init] = fetch.mock.calls[0] as [string, RequestInit];
        expect(calledUrl).toBe(
          "https://api.bridge.nihu.jp/v1/integratedsearch/metadatas/search"
        );
        expect(init.method).toBe("POST");
        expect(
          (init.headers as Record<string, string>)["Content-Type"]
        ).toBe("application/json");
        const body = JSON.parse(init.body as string);
        expect(body.query.paging).toEqual({ start: 10, size: 10 });
        expect(body.query.conditions[0]).toMatchObject({
          connect: "AND",
          query: { term: "源氏物語" }
        });
        expect(result.items[0]!.source).toBe("nihu_bridge");
      });

      it("filters に institute / period / bbox / normalize=false が乗ると body に反映される", async () => {
        const searchFixture = readFixture("search-response.json");
        const fetch = vi.fn().mockResolvedValueOnce({
          ok: true,
          headers: { get: () => "application/json; charset=utf-8" },
          json: async () => searchFixture
        });
        vi.stubGlobal("fetch", fetch);

        const { createNihuBridgeAdapter } = await import(
          "../src/sources/nihuBridge/adapter.js"
        );
        const adapter = createNihuBridgeAdapter();
        await adapter.search({
          query: "平家物語",
          limit: 5,
          page: 1,
          filters: {
            nihu_bridge: {
              institute: ["nijl"],
              normalize: false,
              period_from: "1185",
              period_to: "1600",
              bbox: { lat1: 35.02, lon1: 135.68, lat2: 34.94, lon2: 135.79 }
            }
          }
        });

        const [, init] = fetch.mock.calls[0] as [string, RequestInit];
        const body = JSON.parse(init.body as string);
        expect(body.institute).toEqual(["nijl"]);
        // 第 1 condition: 本文クエリ + normalize=false
        expect(body.query.conditions[0]).toMatchObject({
          connect: "AND",
          query: { term: "平家物語", normalize: false }
        });
        // 第 2 condition: temporal BETWEEN
        expect(body.query.conditions[1]).toMatchObject({
          connect: "AND",
          query: {
            field: "temporal",
            operator: "BETWEEN"
          }
        });
        expect(body.query.conditions[1].query.term).toBe(
          "1185-01-01T00:00:00+09:00,1600-12-31T00:00:00+09:00"
        );
        // 第 3 condition: spatial bbox
        expect(body.query.conditions[2]).toMatchObject({
          connect: "AND",
          query: {
            field: "spatial",
            term: "(35.02,135.68),(34.94,135.79)"
          }
        });
      });

      it("レコード取得は GET で叩き、404 は null に変換する", async () => {
        const recordFixture = readFixture("record-response.json");
        const fetch = vi
          .fn()
          .mockResolvedValueOnce({
            ok: true,
            headers: { get: () => "application/json; charset=utf-8" },
            json: async () => recordFixture
          })
          .mockResolvedValueOnce({
            ok: false,
            status: 404,
            statusText: "Not Found",
            headers: { get: () => "application/json" }
          });
        vi.stubGlobal("fetch", fetch);

        const { createNihuBridgeAdapter } = await import(
          "../src/sources/nihuBridge/adapter.js"
        );
        const adapter = createNihuBridgeAdapter();
        const record = await adapter.getRecord("100000123");
        expect(record?.source_id).toBe("100000123");
        const calledUrl = fetch.mock.calls[0]![0] as string;
        expect(calledUrl).toBe(
          "https://api.bridge.nihu.jp/v1/integratedsearch/metadatas/100000123"
        );

        const missing = await adapter.getRecord("999999999");
        expect(missing).toBeNull();
      });
    });
    ```
  </action>
  <verify>
    <automated>
      cd ndl-jp-lit-mcp && npm test -- --run nihuBridge 2>&1 | tail -30
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/nihuBridge.adapter.test.ts` が存在する。
    - ファイルに `describe("nihuBridge mappers"` が含まれる。
    - ファイルに `describe("createNihuBridgeAdapter"` が含まれる。
    - ファイルに `expect(init.method).toBe("POST")` が含まれる。
    - ファイルに `field: "temporal"` および `field: "spatial"` の文字列が含まれる（filters body のアサーション）。
    - ファイルに `expect(missing).toBeNull()` が含まれる（404 → null フォールバックの検証）。
    - `npm test -- --run nihuBridge` が exit code 0 で成功する（最低 5 ケース通過）。
  </acceptance_criteria>
  <done>nihuBridge mapper / adapter / POST body / 404 フォールバックすべての単体テストが通過</done>
</task>

<task type="auto">
  <name>Task 3: jpLitSearch.test.ts に filters.nihu_bridge の Zod validation テストを追加</name>
  <files>tests/jpLitSearch.test.ts</files>
  <read_first>
    - tests/jpLitSearch.test.ts L733-765 (既存の filters.irdb 同型テスト 4 件 — accept / reject when other source / reject when no source)
    - src/lib/schemas.ts (searchInputSchema.superRefine の実装 — プラン 04-01 で nihu_bridge 用 check を追加済み)
  </read_first>
  <action>
    `tests/jpLitSearch.test.ts` の最後の `it(...)` ブロックの **後** に、以下 4 件のテストを追加する。位置は describe ブロック `describe("createSearchService"` の閉じ `})` の **直前**:

    ```typescript
    it("search 入力スキーマで nihu_bridge source を受け付ける", () => {
      const parsed = searchInputSchema.parse({
        query: "源氏物語",
        source: "nihu_bridge"
      });
      expect(parsed.source).toBe("nihu_bridge");
    });

    it("search 入力スキーマで source=nihu_bridge + filters.nihu_bridge を受け付ける", () => {
      const parsed = searchInputSchema.parse({
        query: "源氏物語",
        source: "nihu_bridge",
        filters: {
          nihu_bridge: {
            institute: ["nijl"],
            normalize: false,
            period_from: "1185",
            period_to: "1600",
            bbox: { lat1: 35.02, lon1: 135.68, lat2: 34.94, lon2: 135.79 }
          }
        }
      });
      expect(parsed.source).toBe("nihu_bridge");
      expect(parsed.filters?.nihu_bridge?.institute).toEqual(["nijl"]);
      expect(parsed.filters?.nihu_bridge?.normalize).toBe(false);
      expect(parsed.filters?.nihu_bridge?.bbox?.lat1).toBeCloseTo(35.02);
    });

    it("search 入力スキーマで source=ndl_catalog + filters.nihu_bridge を reject する", () => {
      const result = searchInputSchema.safeParse({
        query: "源氏物語",
        source: "ndl_catalog",
        filters: { nihu_bridge: { institute: ["nijl"] } }
      });
      expect(result.success).toBe(false);
    });

    it("search 入力スキーマで source なし + filters.nihu_bridge を reject する", () => {
      const result = searchInputSchema.safeParse({
        query: "源氏物語",
        filters: { nihu_bridge: { institute: ["nijl"] } }
      });
      expect(result.success).toBe(false);
    });
    ```

    既存テストは変更しない（jp_lit_search の resolveAdapterOptionsFromEnv テストはプラン 04-03 で既に修正済み）。
  </action>
  <verify>
    <automated>
      cd ndl-jp-lit-mcp && npm test -- --run jpLitSearch 2>&1 | tail -30
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/jpLitSearch.test.ts` に `"search 入力スキーマで nihu_bridge source を受け付ける"` の文字列が含まれる。
    - `tests/jpLitSearch.test.ts` に `"search 入力スキーマで source=ndl_catalog + filters.nihu_bridge を reject する"` の文字列が含まれる。
    - `tests/jpLitSearch.test.ts` に `"search 入力スキーマで source なし + filters.nihu_bridge を reject する"` の文字列が含まれる。
    - `tests/jpLitSearch.test.ts` に `parsed.filters?.nihu_bridge?.bbox?.lat1` の文字列が含まれる。
    - `npm test -- --run jpLitSearch` が exit code 0 で成功する（既存テスト + 新規 4 件すべて通過）。
  </acceptance_criteria>
  <done>filters.nihu_bridge の accept / reject 4 ケースが Zod スキーマレベルで検証される</done>
</task>

</tasks>

<verification>
- `cd ndl-jp-lit-mcp && npm test` が exit code 0（既存 208 件 + 新規 nihuBridge mapper 3 件 + adapter 3 件 + filters validation 4 件）で成功。
- 以下のファイルが存在する:
  - `tests/fixtures/nihu-bridge/search-response.json`
  - `tests/fixtures/nihu-bridge/record-response.json`
  - `tests/nihuBridge.adapter.test.ts`
- 以下の grep がすべてヒットする:
  - `grep -n "nihu_bridge" tests/jpLitSearch.test.ts`
  - `grep -n "expect(init.method).toBe(\"POST\")" tests/nihuBridge.adapter.test.ts`
</verification>

<success_criteria>
- nihuBridge mapper・adapter の単体テストが live API なしで通る。
- POST body の構造（institute / temporal BETWEEN / spatial bbox / normalize=false）がアサートされている。
- 404 フォールバックが検証されている。
- filters.nihu_bridge の Zod validation（accept + reject 2 通り）が検証されている。
- 全テスト（既存 + 新規）が通過。
</success_criteria>
