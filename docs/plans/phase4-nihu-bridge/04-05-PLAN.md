---
plan: "04-05"
title: "smoke test 拡張 + README / mcp-config 更新"
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - scripts/smoke-mcp.ts
  - README.md
  - mcp-config.example.json
autonomous: true
---

<objective>
`npm run smoke:mcp` の live モードで `SMOKE_LIVE_SOURCE=nihu_bridge` を実行可能にし、README に nihu_bridge source 仕様・filters・環境変数を文書化、mcp-config.example.json に環境変数を追加する。

Purpose: ユーザー視点で nihu_bridge を「使える状態」にする。fixture テストと smoke テストでカバレッジを完成させる。
Output: README 更新 + smoke コード拡張 + サンプル mcp-config 更新。
</objective>

<context>
- 既存 smoke は `runLiveSmoke` 関数で 1 source / 1 query を search → getRecord → 必要なら OCR まで貫通する。`nihu_bridge` 専用の追加検証ロジックは不要（既存のフローで `source === "nihu_bridge"` のときも search.items[0] と getRecord(source_id) が貫通すればよい）。
- README は実装状況・対応 source 一覧・引数リファレンス・環境変数の 4 箇所を更新する。
- 既存パターンは irdb / jdcat 追加時の README diff（README L156-172 の filters.irdb 引数表、L331-333 の env 表）を参考。
- mcp-config.example.json に `NIHU_BRIDGE_SEARCH_URL` / `NIHU_BRIDGE_RECORD_BASE_URL` を追加する。
</context>

<tasks>

<task type="auto">
  <name>Task 1: smoke-mcp.ts に nihu_bridge live モード対応を確認</name>
  <files>scripts/smoke-mcp.ts</files>
  <read_first>
    - scripts/smoke-mcp.ts (runLiveSmoke L46-171 の全体構造、source 別分岐 L128-170)
  </read_first>
  <action>
    1. `scripts/smoke-mcp.ts` の `runLiveSmoke` 内、`if (recordData.source === "cinii_books")` / `if (recordData.source === "jstage_articles" && !recordData.title)` の **後** に nihu_bridge 専用の軽い検証を追加する:
       ```typescript
       if (recordData.source === "nihu_bridge") {
         const sourceMeta = recordData.source_metadata as
           | { research_resource_id?: string; database_id?: string }
           | undefined;
         if (!sourceMeta?.research_resource_id) {
           throw new Error(
             "Live smoke nihu_bridge record returned no research_resource_id."
           );
         }
       }
       ```
       これにより `SMOKE_LIVE_SOURCE=nihu_bridge` で実行したときに source_metadata の必須フィールド存在を検証する。
    2. `expectedTools` 配列（L188-196）は変更しない（新ツールは追加していないため）。
  </action>
  <verify>
    <automated>
      cd ndl-jp-lit-mcp && npm run smoke:mcp 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-mcp.ts` に `recordData.source === "nihu_bridge"` の文字列が含まれる。
    - `scripts/smoke-mcp.ts` に `research_resource_id` の文字列が含まれる。
    - `npm run smoke:mcp`（非 live）が exit code 0 で成功し、tools 7 件公開を維持する。
  </acceptance_criteria>
  <done>smoke コードが nihu_bridge live ソースに対応し、非 live モードは既存通り通過</done>
</task>

<task type="auto">
  <name>Task 2: README.md に nihu_bridge を反映</name>
  <files>README.md</files>
  <read_first>
    - README.md (現在の対応 source 表 L29-46、実装状況 L48-111、ツールリファレンス L142-176、環境変数表 L316-339、拡張ロードマップ L443-454)
  </read_first>
  <action>
    1. **冒頭** L3 の「対応一覧」テキストに `nihu_bridge` への言及を追加する（任意。1 行軽く追記）。
       例: `... Japan Search、JDCat、nihuBridge を対象にした...`
    2. **対応 source 表** L27-46 を更新:
       - `## 対応 source（14種）` の見出しを `## 対応 source（15種）` に変更
       - 表に新 1 行を `jdcat` の **後** に追加:
         ```
         | `nihu_bridge` | nihuBridge 統合検索（NIHU 7機関 100+ DB） |  |
         ```
       - L46 の横断検索対象リストは **変更しない**（nihu_bridge は既定対象外）。
    3. **実装状況** L48-111 の最後 `### コンテキスト肥大化を避ける方針` の **前** に新ブロックを追加:
       ```markdown
       - `nihu_bridge` source を追加
         - 検索: nihuBridge 統合検索 API (`POST /v1/integratedsearch/metadatas/search`)
         - detail: nihuBridge メタデータ取得 API (`GET /v1/integratedsearch/metadatas/{researchResourceId}`)
         - NIHU 傘下 7機関 100+ DB を横断（国文研・歴博・国語研・日文研・地球研・民博・機構直轄）
         - 異体字同定（`normalize`、デフォルト ON）に対応
         - 時間範囲（`period_from` / `period_to`、年または ISO8601）と空間検索（`bbox`）に対応
         - 既定横断検索には入れない
       ```
    4. **現状サマリ** L73-87 の文献系 source 列挙にも `nihu_bridge` を追加（jdcat の後）。
    5. **ツールリファレンス jp_lit_search** L156-176 の引数表に新ブロックを追加（既存 `filters.irdb.publisher` の **後**）:
       ```
       | `filters.nihu_bridge` | object | — | `source=nihu_bridge` のときのみ有効 |
       | `filters.nihu_bridge.institute` | string[] | — | 機関ID 配列（`nijl` / `nmjh` / `ninjal` / `ircjs` / `rihn` / `nme` / `nihu`）|
       | `filters.nihu_bridge.database` | string[] | — | DB ID 配列（例: `nijl_nihonkotenseki`）|
       | `filters.nihu_bridge.normalize` | boolean | — | 異体字同定。`false` で明示的にオフ（既定: ON）|
       | `filters.nihu_bridge.period_from` | string | — | 開始時期（ISO8601 / 年）|
       | `filters.nihu_bridge.period_to` | string | — | 終了時期（ISO8601 / 年）|
       | `filters.nihu_bridge.bbox` | object | — | 空間検索 bounding box `{lat1, lon1, lat2, lon2}` |
       ```
       sort 対応状況リスト L164-170 にも `- nihu_bridge: 未対応` を追加。
       filters 対応状況テキスト L172-175 の **後** に同型のブロックを追加:
       ```markdown
       filters.nihu_bridge 対応状況:
       - `source=nihu_bridge` のときのみ有効
       - 横断検索（source 省略時）では使えない
       - `source=nihu_bridge` 以外で指定すると validation error になる
       ```
    6. **環境変数表** L316-339 に 2 行追加（teikoku の **後**）:
       ```
       | `NIHU_BRIDGE_SEARCH_URL` | `https://api.bridge.nihu.jp/v1/integratedsearch/metadatas/search` | nihu_bridge 検索 URL |
       | `NIHU_BRIDGE_RECORD_BASE_URL` | `https://api.bridge.nihu.jp/v1/integratedsearch/metadatas` | nihu_bridge 詳細取得 base URL |
       ```
    7. **拡張ロードマップ** L443-454:
       - 表の `Phase 4` 行の `状況` を `未着手` → `✅ 完了` に変更
       - `**Phase 4 メモ:**` テキスト L454 を完了情報に書き換える:
         ```
         **Phase 4 メモ:** 完了。nihuBridge 統合検索 API（POST JSON）を採用。`filters.nihu_bridge` で institute / database / normalize / period_from・period_to / bbox に対応。既定横断検索には入れない。
         ```
    8. **既知の制約** L413-431 の末尾に nihu_bridge 関連の制約を 1〜3 行追加:
       ```markdown
       - `nihu_bridge` は既定横断検索に含めていません。`japan_search` 経由のデータと重複する可能性があるため、source 指定専用です。
       - `nihu_bridge` の `filters.nihu_bridge.bbox` は緯度経度 bounding box のみ対応です。テキスト地名（「京都」等）での `spatial` 検索は API 側でサポートされていません。
       - `nihu_bridge` の `filters.nihu_bridge.normalize` は既定 `true`（異体字同定 ON）。明示的にオフにする場合のみ `false` を指定してください。
       ```
    9. **fixture 構成表** L463-474 の末尾に 1 行追加:
       ```
       | `tests/fixtures/nihu-bridge/` | nihuBridge search / record 合成 JSON |
       ```
  </action>
  <verify>
    <automated>
      grep -c "nihu_bridge" README.md && grep -c "NIHU_BRIDGE" README.md && grep -q "JDCAT_BASE_URL" README.md && grep -q "TEIKOKU_MEETING_BASE_URL" README.md && echo "existing env rows preserved"
    </automated>
  </verify>
  <acceptance_criteria>
    - `README.md` の `## 対応 source` 見出しに `15種` が含まれる。
    - `README.md` の対応 source 表に `| \`nihu_bridge\` |` の行が含まれる（`grep "| \`nihu_bridge\` |" README.md` がヒット）。
    - `README.md` に `filters.nihu_bridge.institute` の文字列が含まれる。
    - `README.md` に `filters.nihu_bridge.bbox` の文字列が含まれる。
    - `README.md` に `NIHU_BRIDGE_SEARCH_URL` と `NIHU_BRIDGE_RECORD_BASE_URL` がそれぞれ含まれる（環境変数表）。
    - `README.md` の Phase 4 行の状況が `✅ 完了` になっている。
    - `README.md` の fixture 構成表に `nihu-bridge` が含まれる。
    - 既存内容は破壊されていない（既存 source 表の他の行・既存環境変数行が残っている — `grep "JDCAT_BASE_URL" README.md` が依然ヒット）。
  </acceptance_criteria>
  <done>README が nihu_bridge を完全に文書化した状態</done>
</task>

<task type="auto">
  <name>Task 3: mcp-config.example.json に環境変数を追加</name>
  <files>mcp-config.example.json</files>
  <read_first>
    - mcp-config.example.json (現在の env オブジェクトの構造 — README L380-409 とほぼ同じはず)
  </read_first>
  <action>
    `mcp-config.example.json` の `env` オブジェクト末尾に 2 行追加（`TEIKOKU_MEETING_BASE_URL` の **後**、配下キー全体の最後）:
    ```json
    "NIHU_BRIDGE_SEARCH_URL": "https://api.bridge.nihu.jp/v1/integratedsearch/metadatas/search",
    "NIHU_BRIDGE_RECORD_BASE_URL": "https://api.bridge.nihu.jp/v1/integratedsearch/metadatas"
    ```
    末尾なのでカンマの位置に注意（前の行の末尾にカンマが必要、最後の行にはカンマ不要）。

    JSON が壊れないように、`mcp-config.example.json` を読み込んで現状の最後のキーを確認してから追加する。
  </action>
  <verify>
    <automated>
      node -e "JSON.parse(require('fs').readFileSync('mcp-config.example.json','utf8')); console.log('valid')" && grep -c "NIHU_BRIDGE" mcp-config.example.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `mcp-config.example.json` が有効な JSON である（`node -e "JSON.parse(...)"` が成功）。
    - `mcp-config.example.json` に `NIHU_BRIDGE_SEARCH_URL` の文字列が含まれる。
    - `mcp-config.example.json` に `NIHU_BRIDGE_RECORD_BASE_URL` の文字列が含まれる。
    - 既存 env キー（`JDCAT_BASE_URL`, `TEIKOKU_MEETING_BASE_URL` など）は依然存在する。
  </acceptance_criteria>
  <done>サンプル mcp-config に新環境変数が追記された</done>
</task>

</tasks>

<verification>
- `cd ndl-jp-lit-mcp && npm test` が exit code 0 で成功（プラン 04-04 で追加された 7 件 + 既存 208 件）。
- `cd ndl-jp-lit-mcp && npm run build` が exit code 0 で成功。
- `cd ndl-jp-lit-mcp && npm run smoke:mcp` が exit code 0 で成功（tools 7 件公開）。
- `node -e "JSON.parse(require('fs').readFileSync('mcp-config.example.json','utf8'))"` が成功。
- 以下の grep がすべてヒットする:
  - `grep -n "nihu_bridge" README.md`（複数件）
  - `grep -n "NIHU_BRIDGE" README.md`（環境変数表）
  - `grep -n "NIHU_BRIDGE" mcp-config.example.json`（2 件）
  - `grep -n "research_resource_id" scripts/smoke-mcp.ts`
- `cd ndl-jp-lit-mcp && SMOKE_LIVE=1 SMOKE_LIVE_SOURCE=nihu_bridge SMOKE_LIVE_QUERY=源氏物語 npm run smoke:mcp` を実行して exit code 0 を確認する（オプション、ネットワーク必須なので失敗してもブロックしない。Windows PowerShell の場合は `$env:SMOKE_LIVE="1"; $env:SMOKE_LIVE_SOURCE="nihu_bridge"; $env:SMOKE_LIVE_QUERY="源氏物語"; npm run smoke:mcp`）。
</verification>

<success_criteria>
- README に nihu_bridge の対応 source 行 / 引数 / 環境変数 / 制約 / fixture が記載されている。
- mcp-config.example.json に NIHU_BRIDGE_SEARCH_URL / NIHU_BRIDGE_RECORD_BASE_URL が追加されている。
- smoke コードが live `nihu_bridge` 検証に対応している。
- 全テスト・ビルド・非 live smoke が通過。
</success_criteria>
